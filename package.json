{
  "name": "spot-seek",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "backend"
  ],
  "scripts": {
    "test": "npm --prefix app test && npm --prefix backend test",
    "lint": "npm --prefix app run lint && npm --prefix backend run lint",
    "check:bundle": "npm --prefix app run check:bundle && npm --prefix backend run check:bundle"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "drizzle-orm": "^0.45.2",
    "typescript": "~6.0.3"
  }
}
